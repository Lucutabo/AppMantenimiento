<Window x:Class="AppMantenimiento.AsignacionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Asignaciones Equipo - Operario" Height="660" Width="900"
        WindowStartupLocation="CenterOwner" Background="#F0F4F8">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#5A6375"/>
            <Setter Property="Margin" Value="0,0,0,3"/>
        </Style>
        <Style x:Key="BtnAccion" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ColHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#EEF2F7"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#D0D7E3"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- CABECERA -->
        <Border Grid.Row="0" CornerRadius="10" Padding="18,12" Margin="0,0,0,16">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#6366F1" Offset="0"/>
                    <GradientStop Color="#4F46E5" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Asignaciones" FontSize="22" FontWeight="Bold"
                           Foreground="White" VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                <TextBlock Text="Vincula equipos con operarios responsables"
                           FontSize="13" Foreground="#C7D2FE"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- FORMULARIO NUEVA ASIGNACION -->
        <Border Grid.Row="1" Background="White" CornerRadius="10"
                Padding="18,14" Margin="0,0,0,14">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.08"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="Equipo" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="CmbEquipo"
                              Height="34" FontSize="13"
                              BorderBrush="#D0D7E3" BorderThickness="1"
                              DisplayMemberPath="Nombre"
                              SelectedValuePath="Id"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                    <TextBlock Text="Operario" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="CmbOperario"
                              Height="34" FontSize="13"
                              BorderBrush="#D0D7E3" BorderThickness="1"
                              DisplayMemberPath="Nombre"
                              SelectedValuePath="Id"/>
                </StackPanel>

                <Button Grid.Column="2" Style="{StaticResource BtnAccion}"
                        Content="+ Asignar" VerticalAlignment="Bottom"
                        Background="#6366F1"
                        Click="BtnAsignar_Click"/>
            </Grid>
        </Border>

        <!-- TABLA DE ASIGNACIONES -->
        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.08"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="0" x:Name="GridAsignaciones"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserResizeRows="False"
                          RowHeight="38"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          BorderThickness="0"
                          AlternatingRowBackground="#F8FBFF"
                          SelectionMode="Single"
                          ColumnHeaderStyle="{StaticResource ColHeaderStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID"
                                            Binding="{Binding Id}"
                                            Width="60"/>
                        <DataGridTextColumn Header="Equipo"
                                            Binding="{Binding NombreEquipo}"
                                            Width="*"/>
                        <DataGridTextColumn Header="Operario responsable"
                                            Binding="{Binding NombreOperario}"
                                            Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Barra acciones -->
                <Border Grid.Row="1" Background="#F8FBFF"
                        CornerRadius="0,0,10,10" Padding="16,10"
                        BorderThickness="0,1,0,0" BorderBrush="#E5EAF1">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource BtnAccion}"
                                Content="Eliminar asignacion"
                                Background="#DC2626"
                                Click="BtnEliminar_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>